[gd_scene format=3 uid="uid://blvptufbgnn0m"]

[ext_resource type="Texture2D" uid="uid://dtt0jex6wsi3y" path="res://Assets/escaleras/fotografia-de-escada-de-pixel-art-em-um-edificio_811279-94765.jpg" id="1_mipdd"]

[sub_resource type="GDScript" id="GDScript_26vvq"]
script/source = "extends Area2D
@export var landing_zone : Area2D
@export var lower_zone : Area2D
var body_inside := false
var body_ref : Node2D = null
var enemies_inside : Array = []          # ✅ lista de enemigos dentro
static var position_stack : Array = []
var can_use := true

func _on_body_entered(body: Node2D) -> void:
	if !can_use:
		return
	if body.is_in_group(\"player\"):
		body_inside = true
		body_ref = body
	elif body.is_in_group(\"enemy\"):
		if !enemies_inside.has(body):
			enemies_inside.append(body)   # ✅ registrar enemigo
		procesar_enemigo(body)

func _on_body_exited(body: Node2D) -> void:
	if body == body_ref:
		body_inside = false
		body_ref = null
	enemies_inside.erase(body)            # ✅ quitar enemigo si sale físicamente

func _process(delta):
	# Verificación cada frame para el jugador
	if body_ref != null and !overlaps_body(body_ref):
		body_inside = false
		body_ref = null

	# ✅ Verificación cada frame para cada enemigo
	for enemy in enemies_inside.duplicate():
		if !overlaps_body(enemy):
			enemies_inside.erase(enemy)
		else:
			procesar_enemigo(enemy)

	if !body_inside or !can_use or !body_ref:
		return

	if body_ref.is_in_group(\"player\"):
		if Input.is_action_just_pressed(\"teleport\"):
			subir(body_ref)
		if position_stack.size() > 0 and Input.is_action_just_pressed(\"bajar\"):
			bajar()

func procesar_enemigo(body: Node2D):
	if !can_use:
		return                            # ✅ respetar can_use
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	if player.global_position.y < body.global_position.y - 50:
		subir(body)
	elif player.global_position.y > body.global_position.y + 50:
		bajar_enemigo(body)

func subir(body: Node2D):
	if !landing_zone:
		return
	var marker = landing_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	if body.is_in_group(\"player\"):
		if position_stack.is_empty() or position_stack.back() != body.global_position:
			position_stack.push_back(body.global_position)
	var pos = marker.global_position
	if body.is_in_group(\"enemy\"):
		pos.x += 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	if body.is_in_group(\"enemy\"):
		await get_tree().process_frame
		procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar():
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	can_use = false
	var last_pos = position_stack.pop_back()
	player.global_position = last_pos
	if player is CharacterBody2D:
		player.velocity = Vector2.ZERO
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar_enemigo(body: Node2D):
	if !lower_zone:
		return
	var marker = lower_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	var pos = marker.global_position
	pos.x -= 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	await get_tree().process_frame
	procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true
"

[sub_resource type="QuadMesh" id="QuadMesh_p0say"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_nuiet"]
size = Vector2(50, 50.5)

[sub_resource type="GDScript" id="GDScript_bu5ga"]
script/source = "extends Area2D
@export var landing_zone : Area2D
@export var lower_zone : Area2D
var body_inside := false
var body_ref : Node2D = null
var enemies_inside : Array = []          # ✅ lista de enemigos dentro
static var position_stack : Array = []
var can_use := true

func _on_body_entered(body: Node2D) -> void:
	if !can_use:
		return
	if body.is_in_group(\"player\"):
		body_inside = true
		body_ref = body
	elif body.is_in_group(\"enemy\"):
		if !enemies_inside.has(body):
			enemies_inside.append(body)   # ✅ registrar enemigo
		procesar_enemigo(body)

func _on_body_exited(body: Node2D) -> void:
	if body == body_ref:
		body_inside = false
		body_ref = null
	enemies_inside.erase(body)            # ✅ quitar enemigo si sale físicamente

func _process(delta):
	# Verificación cada frame para el jugador
	if body_ref != null and !overlaps_body(body_ref):
		body_inside = false
		body_ref = null

	# ✅ Verificación cada frame para cada enemigo
	for enemy in enemies_inside.duplicate():
		if !overlaps_body(enemy):
			enemies_inside.erase(enemy)
		else:
			procesar_enemigo(enemy)

	if !body_inside or !can_use or !body_ref:
		return

	if body_ref.is_in_group(\"player\"):
		if Input.is_action_just_pressed(\"teleport\"):
			subir(body_ref)
		if position_stack.size() > 0 and Input.is_action_just_pressed(\"bajar\"):
			bajar()

func procesar_enemigo(body: Node2D):
	if !can_use:
		return                            # ✅ respetar can_use
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	if player.global_position.y < body.global_position.y - 50:
		subir(body)
	elif player.global_position.y > body.global_position.y + 50:
		bajar_enemigo(body)

func subir(body: Node2D):
	if !landing_zone:
		return
	var marker = landing_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	if body.is_in_group(\"player\"):
		if position_stack.is_empty() or position_stack.back() != body.global_position:
			position_stack.push_back(body.global_position)
	var pos = marker.global_position
	if body.is_in_group(\"enemy\"):
		pos.x += 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	if body.is_in_group(\"enemy\"):
		await get_tree().process_frame
		procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar():
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	can_use = false
	var last_pos = position_stack.pop_back()
	player.global_position = last_pos
	if player is CharacterBody2D:
		player.velocity = Vector2.ZERO
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar_enemigo(body: Node2D):
	if !lower_zone:
		return
	var marker = lower_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	var pos = marker.global_position
	pos.x -= 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	await get_tree().process_frame
	procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true
"

[sub_resource type="QuadMesh" id="QuadMesh_pw476"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_ywwve"]
size = Vector2(50, 50.5)

[sub_resource type="GDScript" id="GDScript_4ymrd"]
script/source = "extends Area2D
@export var landing_zone : Area2D
@export var lower_zone : Area2D
var body_inside := false
var body_ref : Node2D = null
var enemies_inside : Array = []          # ✅ lista de enemigos dentro
static var position_stack : Array = []
var can_use := true

func _on_body_entered(body: Node2D) -> void:
	if !can_use:
		return
	if body.is_in_group(\"player\"):
		body_inside = true
		body_ref = body
	elif body.is_in_group(\"enemy\"):
		if !enemies_inside.has(body):
			enemies_inside.append(body)   # ✅ registrar enemigo
		procesar_enemigo(body)

func _on_body_exited(body: Node2D) -> void:
	if body == body_ref:
		body_inside = false
		body_ref = null
	enemies_inside.erase(body)            # ✅ quitar enemigo si sale físicamente

func _process(delta):
	# Verificación cada frame para el jugador
	if body_ref != null and !overlaps_body(body_ref):
		body_inside = false
		body_ref = null

	# ✅ Verificación cada frame para cada enemigo
	for enemy in enemies_inside.duplicate():
		if !overlaps_body(enemy):
			enemies_inside.erase(enemy)
		else:
			procesar_enemigo(enemy)

	if !body_inside or !can_use or !body_ref:
		return

	if body_ref.is_in_group(\"player\"):
		if Input.is_action_just_pressed(\"teleport\"):
			subir(body_ref)
		if position_stack.size() > 0 and Input.is_action_just_pressed(\"bajar\"):
			bajar()

func procesar_enemigo(body: Node2D):
	if !can_use:
		return                            # ✅ respetar can_use
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	if player.global_position.y < body.global_position.y - 50:
		subir(body)
	elif player.global_position.y > body.global_position.y + 50:
		bajar_enemigo(body)

func subir(body: Node2D):
	if !landing_zone:
		return
	var marker = landing_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	if body.is_in_group(\"player\"):
		if position_stack.is_empty() or position_stack.back() != body.global_position:
			position_stack.push_back(body.global_position)
	var pos = marker.global_position
	if body.is_in_group(\"enemy\"):
		pos.x += 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	if body.is_in_group(\"enemy\"):
		await get_tree().process_frame
		procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar():
	var player = get_tree().get_first_node_in_group(\"player\")
	if !player:
		return
	can_use = false
	var last_pos = position_stack.pop_back()
	player.global_position = last_pos
	if player is CharacterBody2D:
		player.velocity = Vector2.ZERO
	await get_tree().create_timer(0.3).timeout
	can_use = true

func bajar_enemigo(body: Node2D):
	if !lower_zone:
		return
	var marker = lower_zone.get_node_or_null(\"Marker2D\")
	if !marker:
		return
	can_use = false
	var pos = marker.global_position
	pos.x -= 24
	body.global_position = pos
	if body is CharacterBody2D:
		body.velocity = Vector2.ZERO
	body_inside = false
	body_ref = null
	enemies_inside.erase(body)            # ✅ limpiar de la lista al teletransportar
	await get_tree().process_frame
	procesar_enemigo(body)
	await get_tree().create_timer(0.3).timeout
	can_use = true
"

[sub_resource type="QuadMesh" id="QuadMesh_6ph1a"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_lhmth"]
size = Vector2(50, 50.5)

[node name="Escaleras" type="Node2D" unique_id=1591280616]

[node name="Portal_Escalera" type="Area2D" parent="." unique_id=1639735228]
position = Vector2(-13, -45)
collision_layer = 2
script = SubResource("GDScript_26vvq")

[node name="MeshInstance2D" type="MeshInstance2D" parent="Portal_Escalera" unique_id=1794515673]
position = Vector2(437.73578, 616.6729)
rotation = -3.1403446
scale = Vector2(49.645405, 47.502045)
mesh = SubResource("QuadMesh_p0say")
texture = ExtResource("1_mipdd")

[node name="CollisionShape2D" type="CollisionShape2D" parent="Portal_Escalera" unique_id=1507054241 groups=["portales"]]
position = Vector2(438, 615)
shape = SubResource("RectangleShape2D_nuiet")

[node name="Marker2D" type="Marker2D" parent="Portal_Escalera" unique_id=1557266697]
position = Vector2(437, 614)

[node name="Portal_Escalera2" type="Area2D" parent="." unique_id=193332126]
position = Vector2(-5, -169)
collision_layer = 2
script = SubResource("GDScript_bu5ga")

[node name="MeshInstance2D" type="MeshInstance2D" parent="Portal_Escalera2" unique_id=322093821]
position = Vector2(437.73578, 616.6729)
rotation = -3.1403446
scale = Vector2(49.645405, 47.502045)
mesh = SubResource("QuadMesh_pw476")
texture = ExtResource("1_mipdd")

[node name="CollisionShape2D" type="CollisionShape2D" parent="Portal_Escalera2" unique_id=1707982160 groups=["portales"]]
position = Vector2(438, 616)
shape = SubResource("RectangleShape2D_ywwve")

[node name="Marker2D" type="Marker2D" parent="Portal_Escalera2" unique_id=966823653]
position = Vector2(438, 616)

[node name="Portal_Escalera3" type="Area2D" parent="." unique_id=506918468]
position = Vector2(1, -325)
collision_layer = 2
script = SubResource("GDScript_4ymrd")

[node name="MeshInstance2D" type="MeshInstance2D" parent="Portal_Escalera3" unique_id=1477121897]
position = Vector2(437.73578, 616.6729)
rotation = -3.1403446
scale = Vector2(49.645405, 47.502045)
mesh = SubResource("QuadMesh_6ph1a")
texture = ExtResource("1_mipdd")

[node name="CollisionShape2D" type="CollisionShape2D" parent="Portal_Escalera3" unique_id=285422846 groups=["portales"]]
position = Vector2(438, 617)
shape = SubResource("RectangleShape2D_lhmth")

[node name="Marker2D" type="Marker2D" parent="Portal_Escalera3" unique_id=882975409]
position = Vector2(438, 616)

[connection signal="body_entered" from="Portal_Escalera" to="Portal_Escalera" method="_on_body_entered"]
[connection signal="body_exited" from="Portal_Escalera" to="Portal_Escalera" method="_on_portal_escalera_body_exited"]
[connection signal="body_entered" from="Portal_Escalera2" to="Portal_Escalera2" method="_on_body_entered"]
[connection signal="body_exited" from="Portal_Escalera2" to="Portal_Escalera2" method="_on_body_exited"]
[connection signal="body_entered" from="Portal_Escalera3" to="Portal_Escalera3" method="_on_body_entered"]
[connection signal="body_exited" from="Portal_Escalera3" to="Portal_Escalera3" method="_on_body_exited"]
